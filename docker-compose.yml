services:
  postgres:
    image: postgres:17-alpine
    container_name: comic-library-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-comics}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-comics_dev_password}
      POSTGRES_DB: ${POSTGRES_DB:-comic_library}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-comics} -d ${POSTGRES_DB:-comic_library}"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
